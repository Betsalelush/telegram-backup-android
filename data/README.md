# Telegram Backup Android App

This directory is intentionally empty and will be populated at runtime with:
- `accounts.json` - Saved account credentials
- `transfers.json` - Active transfer configurations
- `sessions/` - Telegram session files
- `progress/` - Transfer progress tracking

**Note:** This directory is excluded from git via .gitignore for security.
