name: Build Android APK (Docker)

on:
  # הפעלה ידנית מהממשק
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to build'
        required: true
        default: 'crash_test'
        type: choice
        options:
          - crash_test
          - basic
          - kivymd
          - full

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
    
    - name: 🎯 Select version
      id: get_version
      run: |
        VERSION="${{ github.event.inputs.version || 'crash_test' }}"
        echo "VERSION=$VERSION" >> $GITHUB_ENV
        echo "version=$VERSION" >> $GITHUB_OUTPUT
        echo "🎯 Building version: $VERSION"
    
    - name: 📄 Copy version files
      run: |
        set -e  # Exit on error
        
        VERSION=$VERSION
        
        case $VERSION in
          crash_test)
            if [ -f "old_versions/main_crash_test.py" ]; then
              cp old_versions/main_crash_test.py main.py
            else
              echo "❌ Error: main_crash_test.py not found"
              exit 1
            fi
            if [ -f "old_versions/requirements_crash_test.txt" ]; then
              cp old_versions/requirements_crash_test.txt requirements.txt
            else
              echo "❌ Error: requirements_crash_test.txt not found"
              exit 1
            fi
            TITLE="Crash Test"
            PACKAGE="crashtest"
            ;;
          basic)
            if [ -f "old_versions/main_test_basic.py" ]; then
              cp old_versions/main_test_basic.py main.py
            else
              echo "❌ Error: main_test_basic.py not found"
              exit 1
            fi
            if [ -f "old_versions/requirements_basic.txt" ]; then
              cp old_versions/requirements_basic.txt requirements.txt
            else
              echo "❌ Error: requirements_basic.txt not found"
              exit 1
            fi
            TITLE="Test Basic"
            PACKAGE="testbasic"
            ;;
          kivymd)
            if [ -f "old_versions/main_test_kivymd.py" ]; then
              cp old_versions/main_test_kivymd.py main.py
            else
              echo "❌ Error: main_test_kivymd.py not found"
              exit 1
            fi
            if [ -f "old_versions/requirements_kivymd.txt" ]; then
              cp old_versions/requirements_kivymd.txt requirements.txt
            else
              echo "❌ Error: requirements_kivymd.txt not found"
              exit 1
            fi
            TITLE="Test KivyMD"
            PACKAGE="testkivymd"
            ;;
          full)
            # Try app/main.py first, then old_versions/main_full.py
            if [ -f "app/main.py" ]; then
              cp app/main.py main.py
            elif [ -f "old_versions/main_full.py" ]; then
              cp old_versions/main_full.py main.py
            else
              echo "❌ Error: main.py not found in app/ or old_versions/"
              exit 1
            fi
            if [ -f "requirements_full.txt" ]; then
              cp requirements_full.txt requirements.txt
            else
              echo "❌ Error: requirements_full.txt not found"
              exit 1
            fi
            TITLE="Telegram Backup"
            PACKAGE="telegrambackup"
            ;;
          *)
            echo "❌ Error: Unknown version: $VERSION"
            exit 1
            ;;
        esac
        
        echo "TITLE=$TITLE" >> $GITHUB_ENV
        echo "PACKAGE=$PACKAGE" >> $GITHUB_ENV
        
        echo "✅ Copied files for version: $VERSION"
        echo "📱 App title: $TITLE"
        echo "📦 Package: $PACKAGE"
        echo "📄 Files copied:"
        ls -lh main.py requirements.txt
    
    
    - name: 🔨 Create buildozer.spec
      run: |
        VERSION=$VERSION
        TITLE="$TITLE"
        PACKAGE="$PACKAGE"
        
        # קביעת requirements לפי גרסה
        if [ "$VERSION" = "crash_test" ] || [ "$VERSION" = "kivymd" ] || [ "$VERSION" = "full" ]; then
          KIVYMD=",kivymd"
        else
          KIVYMD=""
        fi
        
        if [ "$VERSION" = "full" ]; then
          TELETHON=",telethon,pyaes,rsa,pyasn1,openssl"
        else
          TELETHON=""
        fi
        
        cat > buildozer.spec << EOF
        [app]
        title = $TITLE
        package.name = $PACKAGE
        package.domain = org.backup
        source.dir = .
        source.include_exts = py,png,jpg,kv,atlas
        version = 1.0
        
        requirements = python3,kivy==2.2.1,sentry-sdk==1.40.0$KIVYMD$TELETHON
        
        orientation = portrait
        fullscreen = 0
        
        android.permissions = INTERNET,ACCESS_NETWORK_STATE
        android.api = 31
        android.minapi = 21
        android.archs = arm64-v8a
        android.accept_sdk_license = True
        android.logcat_filters = *:S python:D kivy:D
        
        [buildozer]
        log_level = 2
        warn_on_root = 0
        EOF
        
        echo "✅ buildozer.spec created"
    
    - name: 🐳 Build APK with Docker (kivy/buildozer)
      run: |
        echo "🐳 Starting Docker build..."
        echo "⏰ This will take 30-40 minutes"
        echo "📦 Using official kivy/buildozer image"
        
        # הרצת buildozer בתוך Docker container
        docker run --rm \
          -v "$PWD":/home/user/hostcwd \
          -w /home/user/hostcwd \
          kivy/buildozer:latest \
          /bin/bash -c "
            set -eux
            echo '🔧 Upgrading buildozer and python-for-android...'
            pip install --upgrade pip
            pip install --upgrade buildozer python-for-android
            
            echo '🏗️ Building APK...'
            buildozer -v android debug
            
            echo '✅ Build completed!'
          "
    
    - name: 📤 Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: apk-${{ steps.get_version.outputs.version }}-${{ github.sha }}
        path: bin/*.apk
        retention-days: 30
    
    - name: 🎉 Build complete
      run: |
        APK_FILE=$(ls bin/*.apk)
        APK_SIZE=$(du -h "$APK_FILE" | cut -f1)
        echo "✅ Build completed successfully!"
        echo "📦 APK: $APK_FILE"
        echo "📊 Size: $APK_SIZE"
        echo "🔗 Download from: Actions → Artifacts"
